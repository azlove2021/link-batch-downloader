# 办公工具箱 · 大改方案（v2 建议稿）

> 面向真实用户画像：**职场人员，日常用 WPS 处理表格、整理数据、批量改文件名、比对两份数据**。
> 不是给开发者用的工具箱。所有取舍以这个画像为准。
>
> 结论一句话：**35 个工具砍到 15 个，把 6 个做成真好用，比 35 个都"能用"有价值。**

---

## 一、现在的问题（先认清）

| 问题 | 证据 |
|---|---|
| **工具太多，用户做不出选择** | `js/app.js:20-56` 注册了 **35 个工具**，首页平铺 35 张卡片，没有任何分层 |
| **一半工具是给程序员准备的** | JWT、SQL 格式化、正则测试、AES/HMAC、颜色取色…职场用户一辈子不会点开 |
| **该做深的做浅了** | 你日常最需要的三件事（整理数据 / 改文件名 / 对比数据）目前都只是"能用"，没有做到"离不开" |
| **有一个功能是坏的** | `js/extra-tools.js:48` PDF 解密**必然失败**（见 §五-2） |
| **零测试** | 全仓库搜不到任何测试文件，`package.json` 没有 `test` 脚本；而代码里有自研 AES 密文格式，一改就没法解密历史文件 |

---

## 二、删除清单：35 → 15

判据只有一条：**这件事系统/浏览器/WPS 顺手就能做，且做得比你好的，删。**

### 🗑 整块删除（15 个工具）

| 工具 | 代码 | 理由 |
|---|---|---|
| **JWT 查看** | `office.js` 部分 | 开发者工具，你不会用 |
| **SQL 格式化** | `extra-tools.js` 部分 | 同上 |
| **正则测试** | `regexdiff.js` 部分 | 同上；且 VS Code / DevTools 内置 |
| **AES / HMAC** | `aescrypto.js` **129 行** | 开发者工具。真正的需求是"给文件加密码"→ PDF 加密已覆盖 |
| **编码转换** | `encode.js` **148 行** | Base64/URL 编码是开发者需求；你需要的"去换行/去空格/全角转半角"并入**文本处理** |
| **JSON 工具** | `json.js` **96 行** | JSON↔表格的需求并入**表格互转**，格式美化你用不到 |
| **颜色 / 色板** | `extra-tools.js` 部分 | 设计需求，截图取色有 PixPin 等 |
| **时间戳** | `extras.js` 部分 | 转换时间戳是开发者需求 |
| **密码 / UUID** | `extras.js` 部分 | 浏览器和密码管理器自带 |
| **房贷 / 利息** | `office.js` 部分 | 半吊子计算器（缺提前还款/LPR/组合贷），**算出来你不敢信，反而伤害信任** |
| **文本统计** | `office.js` 部分 | WPS 状态栏自带字数统计 |
| **Markdown 编辑器** | `markdown.js` **130 行** | 你用 WPS 写文档，不会用 Markdown |
| **哈希对比** | `extra-tools.js` 部分 | 就是"哈希校验"的一个输入框，独立成工具是菜单膨胀 |
| **PDF 解密** | `extra-tools.js:47-52` | **功能是坏的**（见 §五-2），必须下线 |

**实际执行结果：删除 12 个面板（约 820 行 JS + 约 600 行 HTML）。**
二维码按你的要求保留，见下方第三梯队 #16。

### 🔀 合并（功能不删，收进更合理的位置）

| 原 | 合并到 |
|---|---|
| 图片裁剪 + 长图拼接 | **图片工具**（改成三个标签页：压缩转换 / 裁剪 / 拼接） |
| JSON 工具 | **表格互转** |
| 哈希对比 | **哈希校验**（加一个"粘贴哈希比对"输入框） |
| PDF 加密 | **PDF 工具**（改标签页，只保留加密并修正算法/文案） |
| 统一输出目录 | 不再是"工具"，改成**设置项**（写在设置里，所有导出自动遵循） |

### ⚠️ 特别说明：这三个千万别删

很多人第一反应会删掉它们，但对你（WPS 用户）来说它们是**真需求**：

- **长图拼接** — 聊天记录、审批截图、凭证拼一张，WPS 做不了
- **文件夹归类** — 整理下载文件夹 / 扫描件归档
- **哈希校验** — 交付文件、存档留证的"没被改过"证明

---

## 三、保留的 15 个工具（按你使用的频率排序）

### 🥇 第一梯队：每天可能都用（重仓投入）

| # | 工具 | 现状 | 要做什么 |
|---|---|---|---|
| 1 | **数据工作台** ⭐⭐⭐ | 733 行，虚拟滚动/透视/双表对账已实现 | **做成王牌**：多表合并、按列拆表、清洗方案保存、对账增强（§3.1） |
| 2 | **批量重命名** ⭐⭐⭐ | 177 行，规则+预览已实现 | 扫描件规范化、日期提取重排、冲突检测、**可撤销**（§3.2） |
| 3 | **文本对比** ⭐⭐ | `regexdiff.js` 里最值钱的部分 | 保留并增强：忽略空格/大小写、只看差异、导出差异 |
| 4 | **表格互转** | 94 行 | 吸收 JSON→表格；增加"CSV/Excel 乱码自动识别（GBK/UTF-8）" |

### 🥈 第二梯队：每周会用

| # | 工具 | 现状 | 要做什么 |
|---|---|---|---|
| 5 | **发票提取** ⭐⭐ | 352 行 | **接上已有的 OCR**（§3.3），做成报销闭环 |
| 6 | **PDF 工具** | 304 行 | 加"图片→PDF"（凭证转 PDF）、按页批量拆分；加密修正文案 |
| 7 | **文本处理** | 125 行 | 吸收编码清理 + **脱敏**（手机号/身份证/邮箱） |
| 8 | **图片工具** | 171+326 行 | 合并三合一 + **压缩到指定 KB**（§3.4） |
| 9 | **文件夹归类** | 147 行 | 规则可保存、一键重跑 |
| 10 | **批量下载** | 722 行 | Cookie/Referer、失败清单重跑（§3.5） |

### 🥉 第三梯队：偶尔用，低维护

| # | 工具 | 说明 |
|---|---|---|
| 11 | **哈希校验** | 保留 + 吸收哈希对比 |
| 12 | **图片 OCR**（桌面） | 接发票工具 |
| 13 | **Office→PDF**（桌面） | WPS 转 PDF 有时会跑版，作为平替 |
| 14 | **音视频转码**（桌面） | 录音转 MP3、视频压缩发人 |
| 15 | **大文件清理**（桌面） | C 盘爆满时救命 |
| 16 | **二维码生成** | 按你的要求保留；低频但留着不占地方 |

**另加**：`工具总览`（首页）+ `桌面设置`（设置页，非工具）。

---

## 四、详细改造项

### 3.1 数据工作台 —— 变成"你的 Excel 外挂"

这是你每天最痛的地方，值得投入最多。

**① 多表合并（P0，最急需）**
> 场景：12 个月的月报 CSV，要拼成一张年度总表。

- 一次选多个文件 → 自动识别表头是否一致 → 上下拼接
- **自动加一列「来源文件」**，方便回溯
- 预览前 100 行 + 总行数，确认后再导出

**② 按列拆表（P0）**
> 场景：一张总表，要按「部门」或「月份」拆成 12 个文件发给各负责人。

- 选拆分列 → 预览会生成哪几个文件、各多少行 → 一键导出（**打包 ZIP**）
- 这是 WPS 做起来极其痛苦的事，做出来就是口碑功能

**③ 清洗方案保存（P0）**
> 场景：你每月都要做同一套清洗（去空格 → 日期规范化 → 拆分列 → 去重）。

- 把操作序列存成命名方案（如「月度对账清洗」），下次**一键重跑**
- 方案存在本地，可导出给同事

**④ 对账增强（P0）**
> 场景：财务给的台账和你自己的表对不上，要找出差异。

- **模糊匹配**：名称近似（"有限公司" vs "有限责任公司"）、税号带空格/大小写差异也能对上
- **只看差异**按钮（隐藏一致行）
- **差异逐格高亮**，导出时带一列「差异类型」（仅A / 仅B / 金额不一致 / 名称近似）
- 对账摘要：A 表 N 行、B 表 M 行、匹配 X、仅A Y、仅B Z

### 3.2 批量重命名 —— 针对"扫描件/下载文件"

> 场景：扫描出来一堆 `IMG_20240103_001.jpg`、`扫描件001.pdf`，要整理成
> `2024-01-03_报销单_001.pdf` 这种能看懂的格式。

- **从文件名提取日期**（识别 `20240103` / `2024-01-03` / `IMG_20240103_`）→ 重排到开头
- **序号自动补零**、**关键词替换**、**前后缀**
- **冲突检测**：预览时明确标红"将覆盖/重名"，而不是执行时才报错
- **一键撤销**（保留改名映射表，改错了能退回来）— 这是刚需，改 500 个文件名出错会崩溃

### 3.3 发票报销闭环（P0，资产已存在只差接线）

你后端**已经有** `src-tauri/src/commands.rs:292 ocr_image_powershell`（调用 Windows 自带 OCR，零依赖），但**发票工具完全没用它**，README 还在教你"先用微信识别文字再粘贴"。

要做的：
1. 桌面版：**批量拖入发票图片 → 自动 OCR → 提取字段**（发票号/日期/金额/购销方）
2. **重复检测**：同一发票号出现两次 → 标红（防止重复报销，这是真实事故）
3. **一键出对账表**：所有发票 → 一张汇总表（导出 Excel）
4. **与台账比对**：把公司台账 xlsx 拖进来 → 标出"台账里没有的发票"
5. 全程离线 —— **云端 OCR 要上传发票，全是敏感信息，这是你独有优势**

### 3.4 图片压缩到指定 KB（P1）

> 场景：系统上传限制 2MB，照片有 8MB；或邮件附件超限。

- 输入目标大小（如 500KB）→ **自动迭代压缩质量直到达标**
- 支持批量 + 打包 ZIP
- 这个需求 WPS 免费版做不好，非常高频

### 3.5 批量下载补三个"否则没法用"的缺口（P1）

1. **自定义请求头 / Cookie**（`download.js` 目前只有 `Range`）
   → 内部系统、网盘、带防盗链的链接**现在根本下不了**，这是能不能用的分水岭
2. **失败清单可导出 + 只重跑失败项**
   → 现在只有"重试 3 次"（`download.js:518`）；失败后你要自己翻日志找哪条挂了
3. **下载前预检**
   → 先并发探测所有链接（已有单条探测 `download.js:485`），**下载前就告诉你清单里有多少条是废的**

### 3.6 其它新增（P2）

- **文本脱敏**：手机号/身份证/邮箱一键打码，发给外部前用
- **粘贴清洗**：从网页/PDF 复制来的文本，一键去换行、全角转半角、中英标点统一
- **重复文件查找**：按内容哈希 + 相似文件名，清理重复的照片/文档
- **处理报告**：每次批量操作导出一份"我改了什么"清单（改名前叫什么、对账了多少行）

---

## 五、工程改造

| # | 问题 | 位置 | 修法 |
|---|---|---|---|
| 1 | **零测试**（最高优先级） | 全仓库 | 建 `tests/`，先覆盖纯函数：CSV 解析、日期规范化、金额清洗、去重、对账、重命名规则、密文格式。**不追覆盖率，先防回归** |
| 2 | **PDF 解密是坏的** | `extra-tools.js:48` | pdf-lib 不支持密码解密，`userPassword` 参数被静默忽略 → **解密下线**；加密保留但改文案（算法是 RC4，权限位可绕过，不能说"安全保护"） |
| 3 | **1.5MB 同步阻塞加载** | `index.html:1763-1765` | xlsx(861KB)+pdf-lib(512KB)+markdown-it(120KB) 无 `defer`，串行阻塞。改成**按工具懒加载**，首屏能省 1.5MB |
| 4 | **index.html 1710 行单体** | `index.html` | 35 个工具的 DOM 全堆一个文件。拆成 `views/*.html` 运行时注入（同时解决懒加载） |
| 5 | **版本号三处不一致** | `package.json:4`=1.0.0 / `Cargo.toml:6`=1.1.0 / `tauri.conf.json:4`=1.1.0 | 加 `scripts/bump-version.cjs` 统一 |
| 6 | **README 三处失实** | `README.md` | ①"仅两个库"实为**三个**（xlsx 是最大件）②`链接批量下载器.html` 是 **1KB 跳转页**不是"可单独使用"③没提便携版打包 |
| 7 | **危险操作不可撤销** | 重命名/归类/覆盖 | 执行前自动生成映射/备份，支持一键撤销 |
| 8 | **性能断言没验证** | README 称"10 万行不卡" | 没有 benchmark。补一个可复现测量（生成 10 万行 CSV，记录解析耗时 + 滚动帧率） |

---

## 六、实施路线图

### 第 1 批：止血 ✅ 已完成（commit 8b18b4d）
- [x] PDF 解密下线 + 加密文案修正（明确标注 RC4、不适合高保密文件）
- [x] 三处版本号统一为 1.1.0 + `scripts/bump-version.cjs` 防漂移
- [x] README 三处失实修正
- [x] **建立 `tests/consistency.test.cjs`** 作为后续删减的安全网
      （覆盖：注册表↔面板↔图标↔脚本引用一致性、元素 id 引用有效性、HTML 结构完整性）

### 第 2 批：瘦身 ✅ 已完成（commit 79c3d24）
- [x] `app.js` 工具注册表 35 → 18 项（16 工具 + 首页），新增 `tier` 分层字段
- [x] 删除 12 个面板 + 合并 5 个面板；`index.html` 1791 行 → 1190 行（**−33%**）
- [x] 删除 6 个 JS 文件（约 820 行）；`js/` 从 24 个文件 → **17 个**
- [x] 首页改造：日常常用（4 张大卡）→ 偶尔用 → 更多工具（默认折叠可展开）
- [x] `npm test` 全部通过：**421 项检查**
- [x] 顺带修掉两处历史遗留：多余的 `</section>`、`datawork.js` 的 `#dwCmpKey2` 死引用

### 第 3 批：把主打做深 ✅ 已完成（commits 31dd8e4、714a592、d3b4efb）
- [x] **批量重命名：日期提取重排 / 冲突检测 / 撤销 / 改名记录** ✅（commit 31dd8e4）
      新增「日期提取重排」规则，支持 8 位紧凑、横线/下划线/点/中文、
      含时分秒的长数字串；认不出日期的标红跳过；执行后可撤销 5 步；
      改名记录可导出 CSV。核心逻辑抽到 `js/rename-core.js` 并配 31 个单元测试。
- [x] **数据工作台：多表合并 / 步骤化清洗 / 按列拆表 / 对账增强** ✅（commit 714a592）
      多表合并按**列名**对齐（不是按列位置）：列顺序不同也能拼，缺列留空并提示，
      空行跳过，可加「来源文件」列；合并完可一键载入工作台接着清洗 / 对账。
      清洗从「8 个一键按钮」改成**步骤模式**：操作 + 作用列 + 参数组成一步、可叠加多步；
      先预览（不落地）、确认再执行、执行后可撤销（最多 3 步）；
      列被上游步骤改掉时整步跳过并提示，绝不改错列。
      按列拆表：按某列的值拆成多个 CSV 并打包 ZIP，重名自动加序号，文件名按 Windows 规则合法化。
      双表对账：归一化（全角 / 空格 / 括号 /「有限责任公司」）、忽略大小写、
      可选模糊匹配（相似度阈值可调）、「只看差异」、导出差异表。
      实现：新增 `js/data-core.js`（纯函数核心，66 项单元测试）+ `js/data-batch.js`（界面层）。
- [x] 顺带修掉第 2 批的两处遗留 ✅（同上 commit）
      ① 侧栏菜单写死在 HTML 里，删工具后仍留着 17 个「点进去一片空白」的死入口
         —— 验收标准第 5 条（搜不到指向已删工具的链接）当时其实并不成立；
         现改为由 `js/app.js` 的 `buildSide()` 依据注册表生成，并新增测试守住「每个工具都归了组」。
      ② 删除无人引用的 `js/vendor/markdown-it.min.js`（120KB），vendor 从 3 个减到 2 个。
- [x] 测试规模：`npm test` **688 项**（重命名 31 + 数据核心 66 + 文本核心 33 + 一致性 558）；
      另有一套 jsdom 端到端冒烟测试 42 项（侧栏逐项点击，以及合并 → 载入 → 清洗 → 撤销 → 拆表 → 对账 → 导出全链路）
- [x] **图片：压缩到指定 KB** ✅（commit d3b4efb；三合一在第 2 批已完成）
      「压到 __ KB」：JPG/WebP 二分搜索"能达标的最高质量"（约 7 轮 toBlob）；
      PNG（无质量参数）或质量压到底仍超标时逐档缩小尺寸（每轮 ×0.8，可勾选关闭），
      每轮都从原图重绘、避免多次缩放叠加损失；未达标的图在网格里标 ⚠ 并输出最接近版本；
      批量 + 打包 ZIP 沿用原链路。
- [x] **文本处理：脱敏 / 粘贴清洗** ✅（commit d3b4efb）
      脱敏：手机号（前3后4）/ 身份证（前4后4、生日全遮）/ 邮箱（用户名前2位+完整域名）/ 一键全打码；
      带边界保护——订单号里的长数字不会被误当手机号；顺序为邮箱→身份证→手机号，避免互相拆碎。
      粘贴清洗：去零宽字符、全角转半角（仅字母数字，中文标点故意不动）、中英标点统一、
      合并断行（英文连字符断词 + 中文硬回车，规则保守、数字行/列表不误拼）、一键清洗。
      核心逻辑抽到 `js/text-core.js` 并配 33 个单元测试；顺带修掉 `text.js` 的 esc 未定义 bug。

### 第 4 批：闭环与性能 ✅ 已完成（commits d1a9d9f、f914c69、05f8da5、5d9ffcb；views 拆分评估后暂缓）
- [x] **发票 OCR 闭环** ✅（commit d1a9d9f）
      桌面版「识别图片发票」批量选择 → 接后端 `ocr_image`（Windows 系统 OCR 优先、
      失败回退本机 Tesseract）→ 识别完自动解析；重复发票号自动标红（防重复报销）；
      「载入台账比对」读 xlsx/csv，标出「台账里没有的发票」、支持只看未入账项，
      导出 CSV/Excel 自动带重复/台账列；匹配逻辑抽到 `js/invoice-core.js`（13 项单测）；
- [x] **批量下载增强** ✅（commit f914c69；Cookie/Referer 有浏览器安全边界，见下）
      「预检链接」：8 路并发探测所有勾选链接（GET Range 0-0、单条 10 秒超时），
      404/403/超时/跨域受限在列表里标「预检 ⚠」、悬停看原因，废链不用等下载才发现；
      自定义请求头：`js/download-core.js` parseHeaders 逐行解析「头名: 值」，
      并入预检/扩展名探测/正式下载全部请求。**浏览器硬限制：Cookie/Referer/User-Agent
      等禁止头无法手动设置**——解析层拦截并明确提示（不静默吞掉）；写 `Cookie:` 或勾
      「携带登录 Cookie」自动转 credentials 模式（发送浏览器里该站点已有 cookie）。
      必须伪造自定义 Cookie 值或特定 Referer 的防盗链场景浏览器端无解，
      需桌面端 Rust 下载器（reqwest，可绕开头限制与 CORS）——涉及新后端命令+进度事件，建议单独立项；
      失败重跑/清单：「重跑失败项」一键只选失败/中断项重下；「导出失败清单」输出与
      parseTxt 兼容的 txt（标题+Tab+链接），放回源文件夹重新扫描即可再下；
      Excel 导出新增「失败原因」「预检」两列；单元测试 16 项（tests/download-core.test.cjs）。
- [x] **vendor 按需懒加载** ✅（commit 05f8da5）
      xlsx(861KB) + pdf-lib(512KB) 不再随首屏同步加载，改为工具首次使用时由
      `core.js` 的 `TB.loadVendor` 动态注入 <script>（file:// 双击场景下 script 注入有效，fetch 才受 CORS 限制）；
      接入点：PDF 工具箱全部入口（页数统计/合并/拆分/旋转/页码/提取/信息/加密）、
      工作台 xlsx 导入(parseAnyFile)+导出、多表合并（复用 parseAnyFile）、发票台账载入；
      加载失败明确报错可重试，不再静默降级。**首屏实测 349KB**（index.html+css 91KB + js 根目录 258KB），
      验收标准 3（1.5MB→400KB 以内）达成；npm test 724 项通过。
- [ ] `index.html` 拆 `views/`（**暂缓，建议不做**）：运行时注入 views 需 fetch 本地文件，
      file:// 双击打开时被浏览器 CORS 拦截，直接违背「双击 index.html 就能用」约束；
      且体积大头在 vendor（已解决），拆 views 对首屏体积无增益、只剩 HTML 转 JS 字符串的转义/一致性风险。
      若日后放弃 file:// 双击场景（只留 Tauri 桌面版），可再评估。
- [x] **性能基准实测** ✅（commit 5d9ffcb）
      `npm run bench`（`scripts/bench-data.cjs`）：固定种子生成 10.5 万行 × 8 列 CSV
      （10.1MB，引号/中文/三种日期格式混排/5% 重复行），每项跑 3 轮取中位数，
      带数量级防回归守卫（超守卫 exit 1）。实测（i9-13900HX / Node 26.7 / win32）：
      parseCsv **141ms** · 4 步清洗流水线 169ms · toCsv 序列化 77ms ·
      mergeTables 10.5万+5万按列名合并 37ms · groupRowsBy 拆 3000 组 9ms ·
      similarity 20 万对模糊匹配 635ms ——「10 万行不卡」有可复现数据支撑，README 已标注。
      滚动帧率属浏览器渲染指标 Node 测不了，脚本头部注明手工验证方式。

### 第 5 批：P2 收尾 ✅ 已完成（commits b268d4b、89ee438、24650db）
- [x] **危险操作可撤销**（工程改造 #7 收尾）：文件夹归类新增「撤销上次归类」
      （会话内最多 5 步、倒序把文件移回原位、搬空的子文件夹自动清理）+「导出归类记录」CSV；
      规则分桶/计划汇总/报告行抽到 `js/folder-core.js`（27 项单测）。
      至此三类危险操作全部可撤销：重命名（5 步）、清洗（3 步）、归类（5 步）；
      重复文件隔离（本批新增）从设计上只移动不删除。
- [x] **重复文件查找**（§3.6）：递归扫描（跳过隐藏文件）→ 按大小分组 → 前 64KB 快筛哈希 →
      全量 SHA-256，两级过滤少读盘；相似文件名用 data-core 的 Dice 相似度，
      按归一化名排序后只比邻域窗口（10 个），避免 O(n²)；
      处置只移动不删除——副本移入「_重复文件/组N/」可一键撤销，清单可导出 CSV；
      每组保留「层级最浅/路径最短」的那个（规则稳定可预期）。`js/dup-core.js`（18 项单测）。
- [x] **处理报告**（§3.6）：`TB.util.oplog` 会话操作日志——载入/清洗执行/清洗撤销/多表合并/
      按列拆表/双表对账/文件夹归类（含行数变化与明细），工作台「导出处理报告」一键 CSV；
      加上已有的重命名记录、归类记录，「我改了什么」全部可留档。
- [x] 测试共 **813 项**（6 套纯函数单测 + 一致性 609）；工具注册表 17 → **18 个**。
- 遗留：views 拆分暂缓（file:// CORS 冲突）；Rust 下载器 → 已在第 6 批立项完成（见下）。

### 第 6 批：桌面原生下载通道 ✅ 已完成（commit 2eb0c99；桌面版需重新构建生效）
- [x] **Rust 下载器**（第 4 批遗留项立项）：`src-tauri` 新增 5 个命令——
      `http_download`（reqwest 流式下载：请求头原样发送，**Cookie/Referer/User-Agent
      等浏览器禁止头全部生效、不受 CORS 拦**；自动 Range 续传、416 视为已完整、
      服务器不支持续传→自动从头下；自动建目录；`http-progress` 事件每 ≥100ms 推一次）、
      `http_cancel`（取消标志表，半截文件保留可续传）、`file_size`（跳过/续传判断）、
      `sha256_file`（原生路径文件浏览器句柄够不到，校验改由 Rust 计算）、`save_text`（落 _checksums）。
- [x] **前端接入**：批量下载页新增「原生下载通道」勾选 +「原生输出」目录（仅桌面壳里显示；
      浏览器句柄没有真实路径，故输出用系统对话框另选目录）；勾选后全部请求走 Rust 通道，
      禁止头原样发送（开启时明确提示哪些头会生效）；跳过/续传/并发/重试×3/SHA-256/
      _checksums.sha256.txt 与浏览器通道对齐。`parseHeaders` 新增 `raw` 字段（含禁止头，原生通道用）。
- [x] **已知差异**（代码注释有记录）：暂停只在任务之间生效（不打断进行中的任务）；
      扩展名 MIME 探测跳过（文件名保持扫描时的推断）。
- [x] **验证**：npm test **820 项**（download-core 16→18、一致性 609→614）；`cargo check` 通过（exit 0）；
      网页双击模式完全不受影响（无 `window.__TAURI__` 时入口、UI、通道全部保持原样）。

### 第 7 批：补齐计划缺口 ✅ 已完成
对照 §四 / §五 清点后，发现下列项在前 6 批漏做，本批补齐：
- [x] **清洗方案保存 / 一键重跑**（§3.1③，P0）：数据工作台清洗区新增命名方案——
      存本机 localStorage、可删可载入；载入时**按列名映射**到当前表头，
      对不上的列整步跳过并提示（与执行引擎同一原则：宁可不动）；
      **导出/导入 JSON**，方便发给同事套用（如「月度对账清洗」）。
- [x] **PDF：图片 → PDF**（§三-6）：多张扫描件/凭证按选择顺序合并成一个 PDF；
      可选「按图自适应」或 A4 纵向，可设边距；Canvas 抽帧为 JPEG 再 embed，避免格式不兼容。
- [x] **文件夹归类：规则记忆**（§三-9）：归类规则（模式/关键字/跳过已有子文件夹）
      自动存本机；重开工具静默恢复；显式按钮「载入上次规则」便于回看。
- [x] `npm test` 全绿：**839 项**（一致性 614→633，因新增元素 id 全部入册）。

### 仍暂缓 / 不做（有意决策，不是漏做）
- [ ] `index.html` 拆 `views/`（**建议不做**）：`file://` 下 fetch 本地 HTML 被 CORS 拦截，
      与「双击就能用」冲突；vendor 已懒加载，首屏体积问题已解决。
- [ ] 浏览器端伪造 Cookie / Referer：浏览器安全模型禁止，无解；
      需要时走桌面版「原生下载通道」（第 6 批已做）。
- [ ] 云端 OCR / 在线翻译等：与「离线、发票不上传」定位冲突，明确不进本产品。

### 验收标准
1. 首页一屏之内能找到你最常用的 4 个工具
2. 常用操作（合并表、对账、改名）**不超过 3 步**
3. 首屏加载体积从 1.5MB 降到 400KB 以内
4. `npm test` 可跑，核心纯函数有回归用例
5. 全仓库搜不到指向已删工具的链接

---

## 七、风险与保底

- **删除是破坏性操作**：仓库是 git（`main` 分支，工作树干净），每批改动独立提交，随时 `git revert`
- **不做的事**：不引入构建工具链（保持"双击 index.html 就能用"这个核心优势）、不上云、不改数据模型
- **如果某批改坏了**：`git log` 里每批一个 commit，回退到上一批即可

---

## 八、后续路线（第 8 批起，按价值排序）

> 原则不变：**职场 WPS 用户、离线优先、先做深再做宽**。下面尚未开工，按建议优先级。

### 第 8 批：数据工作台再上一个台阶 ✅ 已完成
| # | 项 | 说明 |
|---|---|---|
| 8.1 | **透视汇总增强** | 透视表旁可一键「柱状图 / 饼图」（Canvas，可下载 PNG）；基于透视或交叉表合计 |
| 8.2 | **交叉表（数据透视表样式）** | 行×列矩阵，如部门×月份；求和/平均/计数；行列合计；导出 CSV |
| 8.3 | **公式列** | `税额 = [金额] * 0.13`、`合计 = 数量 * 单价`；按列名引用，¥/千分位自动清洗；非法公式拒绝；单测覆盖注入防护 |
| 8.4 | **图表柱/饼** | 8.1，不引入重型图表库，保持离线与体积 |

实现：`js/data-formula.js`（parseFormula / evalFormula / applyFormulaColumn / crosstab）+  
`tests/data-formula.test.cjs`（14 项）。`npm test` **全绿**，一致性 653 项。

### 第 9 批：发票与资料闭环加强
| # | 项 | 说明 |
|---|---|---|
| 9.1 | **OCR 批量结果回写列表** | 桌面版多图识别完，进度条 + 失败重识别单条 |
| 9.2 | **发票汇总表模板** | 按月份/供应商自动汇总，导出「报销单」样式 Excel |
| 9.3 | **重命名 → 归类流水线** | 先日期重排再按年月归类，一条命令跑完 |
| 9.4 | **重复文件：按目录对比** | 指定 A/B 两个文件夹，找「只在一边的文件」 |

### 第 10 批：体验与分发（低风险）
| # | 项 | 说明 |
|---|---|---|
| 10.1 | **首次运行向导** | 打开首页显示「你要做哪件事」三选：整理表格 / 改文件名 / 对账 |
| 10.2 | **工具使用提示** | 数据工作台关键步骤旁「为什么 / 注意什么」可展开 |
| 10.3 | **便携包自动更新检查** | 可选：本地版本号 vs GitHub Release，仅提示不自动下载 |
| 10.4 | **桌面版 Release 固化** | 仓库 Release 附带便携 zip + setup + SHA256 |

### 明确不做（与定位冲突或浏览器做不到）
- 云端同步、账号体系  
- 浏览器里伪造 Cookie/Referer（走桌面原生通道）  
- 完整 Excel 函数引擎（那是 WPS）；只做办公够用的「计算列」  
- 音视频剪辑时间线（只保留转码）

### 怎么继续开发
1. 打开本项目：`E:\AAA_AI\SunCode\Project_010_链接批量下载器`  
2. 改完跑：`npm test`（全绿再提交）  
3. 网页验收：双击 `index.html`  
4. 桌面：`npm run build` 产出便携包/安装包到 `dist/`  
5. 每批改动在本文件勾选 `[x]`，并写清 commit 与测试规模  

---

## 九、进度总表（截至第 7 批）

| 批次 | 内容 | 状态 |
|---|---|---|
| 1 | 止血：PDF 解密下线、版本统一、README、一致性测试 | ✅ |
| 2 | 瘦身：35→18 工具、删面板/JS、首页分层 | ✅ |
| 3 | 打深：重命名撤销、数据合并/拆表/清洗/对账、图片压到 KB、文本脱敏 | ✅ |
| 4 | 闭环：发票 OCR+台账、下载预检/自定义头/失败重跑、vendor 懒加载、bench | ✅ |
| 5 | 收尾：归类撤销、重复文件、处理报告 | ✅ |
| 6 | 桌面 Rust 原生下载通道 | ✅（exe 需用当前代码重新 build） |
| 7 | 补缺：清洗方案保存、图片→PDF、归类规则记忆 | ✅ |
| 8 | 交叉表、公式列、透视/交叉表图表 | ✅ |
| 9+ | 发票流水线、体验向导 | 📋 见上方路线 |

**质量**：`npm test` 839 项；`npm run bench` 10 万行可复现；首屏约 349KB。  

---

## 十、一句话总结

你现在有 **19 个注册入口里的高价值工具**（核心 6 个已做到可依赖），  
第 7 批把计划里漏掉的 **清洗方案 / 图片转 PDF / 归类规则记忆** 补齐。  
下一步若继续，优先做 **第 9 批（发票批量 OCR 进度、报销汇总、重命名→归类流水线）**——  
那是报销与资料整理闭环。第 8 批（交叉表、公式列、图表）已完成。

