{
  "name": "office-toolbox-desktop",
  "private": true,
  "version": "1.1.0",
  "description": "Office toolbox desktop shell (Tauri)",
  "type": "module",
  "scripts": {
    "test": "node tests/rename-core.test.cjs && node tests/data-core.test.cjs && node tests/text-core.test.cjs && node tests/consistency.test.cjs",
    "bump": "node scripts/bump-version.cjs",
    "sync": "node scripts/sync-web.cjs",
    "dev": "npm run sync && tauri dev",
    "build": "npm run sync && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
