{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "默认能力：窗口 + 本地页面",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
