# 办公工具箱

本地离线的多功能网页工具箱：批量下载、文本整理、格式转换、发票字段提取……  
**一个入口，双击 `index.html` 就能用。** 不用装 Python、不用起服务、不用上传数据。

> 原「链接批量下载器」已完整保留为其中一项工具，并扩展了十余个办公常用能力。

---

## 快速开始

1. 下载/克隆本仓库
2. **双击 `index.html`**（推荐 Chrome / Edge 86+）— 网页版
3. 左侧选工具，用完即走

### 桌面版（exe）

两种分发方式，按需选择：

| 方式 | 文件 | 说明 |
|---|---|---|
| **便携版（免安装）** | `dist/办公工具箱-便携版.zip`（约 38MB） | 解压后双击 `办公工具箱.exe` 即可 |
| **安装包** | `dist/办公工具箱_1.1.0_x64-setup.exe`（约 28MB） | 可选装到开始菜单 |

便携版请**整夹拷贝**（必须带 `resources\`，否则转码不可用）。二者都内置 FFmpeg；OCR 默认用 Windows 系统识别；Tesseract / LibreOffice 见「桌面设置 → 可选增强组件」。

从源码打包：`npm install` 后执行 `npm run build`（会先 `npm run sync` 把网页资源同步到 `web/`），产物在 `dist/`。详见 [BUILD_TAURI.md](BUILD_TAURI.md)。

桌面版能力：

| 功能 | 依赖 |
|---|---|
| 音视频转码 | **内置 FFmpeg** |
| 图片 OCR | **Windows 系统 OCR**（PixPin 同类；可选装 Tesseract 增强） |
| Office → PDF | 可选安装 LibreOffice（应用内提供下载链接） |
| 托盘 / 右键菜单 / 大文件清理 | 无额外依赖 |

打开 **桌面设置 → 可选增强组件**，即可看到 Tesseract、LibreOffice、FFmpeg 完整版的官方下载链接。

自己从源码打包见 **[BUILD_TAURI.md](BUILD_TAURI.md)**。

所有处理都在浏览器/本机完成，**文件与文本不会上传到任何服务器**。

---

## 工具一览

18 个工具 + 首页总览，本表与 `js/app.js` 注册表保持一致（`npm test` 一致性校验会盯住）。标注「桌面版」的工具需安装 Tauri 桌面版。

### 数据与表格（主打）
| 工具 | 说明 |
|---|---|
| **数据工作台** | **约 10 万行 CSV/XLSX 虚拟滚动不卡**（`npm run bench` 实测：解析 10.5 万行中位 141ms）；多列联合筛选（`!` 可排除）；按列名**多表合并**；步骤化清洗（去空格/规范日期/金额/去重，可预览可撤销）；**按列拆表**打包 ZIP；**双表按主键比对**（归一化/模糊匹配）导出仅A/仅B/字段差异；**透视汇总**（分组求和/平均/计数）；**处理报告**（会话内全部操作导出 CSV 留档） |
| **表格互转** | CSV / TSV / JSON / Markdown / Excel |
| **文本处理** | 多行→一行、去重、排序、提取号码链接；**脱敏打码**（手机号/身份证/邮箱）；**粘贴清洗**（去换行/全角转半角/中英标点/零宽字符） |
| **文本对比** | 行级 Diff，新增/删除高亮 |

### 文件与整理
| 工具 | 说明 |
|---|---|
| **批量下载** | 扫描文件夹里所有 txt 的下载链接，按文件名自动分目录；**预检链接**（下载前标出废链）、**自定义请求头/登录 Cookie**、断点续传、并发、SHA-256、**重跑失败项/导出失败清单**、导出 TXT/Excel；桌面版可勾**「原生下载通道」**——Rust/reqwest 下载，Cookie/Referer/UA 等浏览器禁止头原样发送、不受跨域限制 |
| **批量重命名** | 日期提取重排/替换/前后缀/编号，规则先预览、**可撤销 5 步**、改名记录导出 CSV |
| **文件夹归类** | 按扩展名 / 修改年月 / 关键字 / 大小档自动归子文件夹，先预览再执行；**可撤销上次归类**、导出归类记录 CSV |
| **哈希校验** | 本地文件/文件夹 SHA-256 / MD5，可写 `_checksums.sha256.txt`；内置**哈希对比**（两段哈希或两个文件） |
| **重复文件查找** | 按内容 SHA-256 找完全重复（大小分组 + 64KB 快筛 + 全量哈希，少读盘）；按文件名找相似（Dice 相似度）；副本可移入「_重复文件」隔离（**不删除、可撤销**），清单导出 CSV |

### 格式转换
| 工具 | 说明 |
|---|---|
| **图片工具** | PNG / JPG / WebP 互转、质量压缩、**压缩到指定 KB**、最长边缩放、右下角水印、**裁剪**（等比例锁）、**长图拼接**，打包 ZIP 下载 |
| **PDF 工具** | 合并、拆分、旋转、加页码、提取页、查看信息、**加打开密码**（本地 pdf-lib，按需加载） |
| 音视频转码（桌面版） | 本机 FFmpeg 转换（mp4 / mp3 / gif …） |
| Office→PDF（桌面版） | LibreOffice 转 Word / Excel / PPT 为 PDF |

### 办公实用
| 工具 | 说明 |
|---|---|
| **发票提取** | 从发票文字 / 带文字层 PDF 提取：发票号码、开票日期、价税合计、购销方、税额等；桌面版支持批量**图片发票 OCR**（Windows 系统识别，离线）；自动**重复发票号检测**；载入台账 xlsx/csv **标出台账里没有的发票**；导出 CSV/Excel 带比对结果 |
| 二维码生成 | 文本/链接 → 二维码 PNG，可调纠错与颜色 |
| 图片 OCR（桌面版） | 扫描件识别文字（Windows 系统 OCR 优先，本机 Tesseract 兜底） |
| 大文件清理（桌面版） | 扫描大文件移入回收站（可恢复） |
| 桌面设置（桌面版） | 托盘、右键菜单、依赖检测、**统一输出目录** |

首页支持**工具搜索**（也可按 `/` 快捷聚焦）。

---

## 批量下载（原功能保留）

```
文档清单/
├── R5300.txt          <- 原始清单
├── R5300/             <- 自动创建，存放 R5300.txt 里的所有下载
│   ├── 产品白皮书.pdf
│   └── ...
```

支持 txt 四种写法：纯链接 / 序号+标题+链接 / 标题行+链接行 / Markdown 链接。  
同名策略：续传（推荐）/ 跳过 / 重新下载。详见工具内「帮助」折叠说明。

**必须用 Chrome / Edge**（File System Access API）。Firefox / Safari 无法读写本地文件夹，其余文本类工具仍可用。

---

## 发票识别说明

**完全离线**——发票全是敏感信息，全程不出本机：

1. **电子发票 PDF**（有文字层）→ 点「从 PDF 提取文字」通常可直接解析  
2. **扫描件 / 图片（桌面版）** → 点「识别图片发票」批量选择，自动调用 **Windows 系统 OCR**（无需下载模型；系统识别失败时回退本机已装的 Tesseract），识别完自动解析  
3. **网页版** → 先用其它工具 OCR 成文字再粘贴；多张文字用空行或 `====` 分隔，或拖入多个 `.txt`  
4. **防重复报销** → 解析后自动检测重复发票号并标红；「载入台账比对」可用公司台账标出「台账里没有的发票」，导出自动带上重复/台账列

自动字段：发票代码/号码、开票日期、校验码后 6 位、购销方名称与税号、金额、税额、价税合计（含大写）。

---

## 目录结构

```
Project_010_链接批量下载器/
├── index.html          # 入口（双击打开）
├── css/style.css
├── js/
│   ├── core.js          # 公共库：通知 / 哈希 / 手写 XLSX·ZIP / vendor 按需加载
│   ├── app.js           # 工具注册表、侧栏导航、首页搜索
│   ├── data-core.js     # 数据工作台纯函数核心（合并/拆表/清洗流水线/对账）
│   ├── datawork.js      # 数据工作台界面
│   ├── data-batch.js    # 多表合并 / 按列拆表 批量入口
│   ├── convert.js       # 表格互转
│   ├── text-core.js     # 文本处理纯函数核心（脱敏 / 粘贴清洗）
│   ├── text.js          # 文本处理
│   ├── diff.js          # 文本对比
│   ├── rename-core.js   # 批量重命名纯函数核心（日期识别 / 模板）
│   ├── rename.js        # 批量重命名
│   ├── folder-core.js   # 归类纯函数核心（规则分桶 / 汇总 / 记录报告）
│   ├── folderorg.js     # 文件夹归类（可撤销 / 导出记录）
│   ├── dup-core.js      # 重复文件纯函数核心（大小分组 / 保留选择 / 相似名配对）
│   ├── dupfind.js       # 重复文件查找
│   ├── download-core.js # 批量下载纯函数核心（请求头解析 / 失败清单）
│   ├── download.js      # 批量下载
│   ├── hash.js          # 哈希校验（含哈希对比）
│   ├── image.js         # 图片工具（转换 / 压缩 / 压到指定 KB）
│   ├── imgedit.js       # 图片裁剪 / 长图拼接（已并入图片工具面板）
│   ├── pdf.js           # PDF 工具（含加打开密码）
│   ├── invoice-core.js  # 发票纯函数核心（重复检测 / 台账比对）
│   ├── invoice.js       # 发票提取
│   ├── qrcode.js        # 二维码生成
│   ├── outdir.js        # 统一输出目录
│   ├── desktop.js       # 桌面版专属（FFmpeg / LibreOffice / OCR / 清理 / 托盘）
│   └── vendor/          # 本地第三方：pdf-lib、xlsx（均按需加载，不占首屏）
├── scripts/              # sync-web / bump-version / bench-data（性能基准）等维护脚本
├── tests/                # 纯函数核心单测 + 全仓库一致性校验（npm test，820 项）
├── src-tauri/            # Tauri 桌面壳（打包 exe）
├── package.json
├── BUILD_TAURI.md
├── 改进建议.md            # 改进路线图（四批进度与决策记录）
├── dist/                 # 本地生成的安装包（gitignore）
├── 链接批量下载器.html  # 升级引导页（自动跳转 index.html，可删）
├── README.md
└── LICENSE
```

旧的 `链接批量下载器.html` 现在是**升级引导页**：打开后提示已改名为「办公工具箱」，并自动跳转到 `index.html`。它本身不再包含任何功能，入口统一走 `index.html`。

---

## 技术说明

| | |
|---|---|
| 语言 | 原生 HTML + CSS + JS，**零构建**；库文件本地 vendor，无外网 CDN |
| 文件读写 | File System Access API（下载/哈希/文件夹选图） |
| 下载 | `fetch` + `ReadableStream` 流式落盘 + Range 续传；桌面版可切 Rust/reqwest 原生通道（请求头无浏览器限制） |
| Excel / ZIP | 浏览器 `CompressionStream` 手写 OOXML / ZIP |
| 二维码 | 内置精简 QR 编码器（byte 模式，纠错 L/M/Q/H） |
| PDF 编辑 | [pdf-lib](https://github.com/Hopding/pdf-lib)（本地 vendor） |
| PDF 文字 | 内置轻量解析（FlateDecode + ToUnicode），电子发票文字层可用；扫描件桌面版内置系统 OCR |
| 额外依赖 | `js/vendor/` 下 **2** 个库：pdf-lib（512KB）、xlsx（861KB）。**按需动态加载**（工具首次使用时注入，首屏约 349KB），全部本地化，仍可完全离线双击使用 |
| 质量保障 | `npm test`：纯函数核心单元测试 + 全仓库一致性校验（820 项）；`npm run bench`：10 万行数据链路可复现性能基准 |

---

## 常见问题

**Q：为什么不上传文件？**  
A：设计目标是「本地离线」。所有计算都在你电脑的浏览器里完成，适合处理带隐私的发票、清单、内部文档。

**Q：Firefox 打不开批量下载？**  
A：Firefox 不支持 File System Access API。请用 Chrome / Edge。

**Q：图片发票怎么提取？**  
A：桌面版在「发票提取」点「识别图片发票」批量调用 Windows 系统 OCR（离线、零依赖）；网页版需先用其它工具识别成文字再粘贴。

**Q：能拷到 U 盘给别人吗？**  
A：可以。整个文件夹拷走即可，注意保持 `index.html` 与 `css/`、`js/` 的相对路径。

---

## License

MIT
